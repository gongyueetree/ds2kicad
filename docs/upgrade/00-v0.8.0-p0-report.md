# P0 修复报告（对应审计 v0.7.5 → v0.8.0）

## 已完成（含测试证据）
- **P0-1 切片页码映射**：`remapDerivedPages()` 将 Gemini 输出的 figure.page / packages[].sourcePages / landPattern.sourcePage 反向映射回原文页码；无法映射的引用一律剔除（禁止错误证据）。测试：非连续切片 [1,5,9] + 越界剔除。
- **P0-2 禁默认猜测**：提示词删除 "use JEDEC-standard nominal"（改为 output null，NEVER estimate）；`sanitizePackage` 记录 `missingFields` 且二次 sanitize 不冲掉证据；pitch+本体缺失 → `blocked_missing_geometry` 阻断产物；部分缺失/JEDEC 先验修正 → `placeholder` + bundle/part-bundle `nonPromotable=true`。测试：blocked/placeholder/可晋升三态。
- **P0-3 mock fail-closed**：缺 Key 且未显式 `MOCK_MODE=1` → 503 `model_not_configured`；mock 响应带 `non_promotable=true`。冒烟覆盖。
- **P0-4 SafeDownloader**：`lib/safedl.js` 逐跳 manual redirect（≤5）+ 每跳 scheme/端口白名单 + DNS 解析后私网/保留/CGNAT/元数据/v4-mapped-v6（点分与十六进制双形态）拒绝 + 流式真实字节上限。Edge 代理补流式字节上限。测试：13 类受限 IP + 端口/协议/映射地址 URL 校验。
- **P0-5 postMessage**：入站 origin 白名单（`VITE_EZPLM_ORIGINS`）、出站精确 targetOrigin；未配置仅开发放行并告警。
- **P0-6 接口边界**：可选 `API_TOKEN` Bearer 认证（extract/generate），前端 `VITE_API_TOKEN` 注入。
- **Agent4 候选保留**：`filterFiguresDetailed` 返回被拒候选+原因（`figureCandidates`），不再永久丢弃。
- **pdf-inspector 适配器**：`lib/parsers/pdfInspectorAdapter.js`，`PDF_PARSER=inspector` 显式启用（Vercel 默认 pdfjs）。实证修正两处上游语义：extractTextWithPositions 返回扁平数组、页码 1-based、y 为左下原点（与 README 区域 API top-left 不同）——契约测试钉死。
- 版本统一 0.8.0；NOTICE.md；.env.example 全量补充。

## 明确未做（NOT VERIFIED / 后续阶段）
- 作业队列/PostgreSQL/对象存储/Canonical IR/Schema Registry/Evidence Anchor 完整对象模型（Phase 1-2，应在 ezPLM 仓库立项，复用其鉴权/存储）。
- KiCad CLI 实测、Golden Fixture、DRC、STEP、视觉回归（Phase 3）。
- 200 份真实数据手册 benchmark（需固定语料库与许可）。
- DNS rebinding 残余时间窗（需按解析 IP 直连，undici 定制，Worker 阶段）。
- 真实 Gemini 调用与多厂商回归：本环境无 Key，NOT VERIFIED。
