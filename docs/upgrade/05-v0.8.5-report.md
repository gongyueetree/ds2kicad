# v0.8.5 修复报告（生产闭环）

版本统一：package.json / package-lock.json 均为 0.8.5。未新增封装类型，未新增 UI 功能。

## 13 项逐条

| # | 落地 | 证据 |
|---|---|---|
| 1 | mock 响应删除重复 `packages` 键（此前第二个键覆盖第一个、丢失稳定 ID），三分支（mock/live/degraded）统一**先落库再从 Job IR 回读**；live 分支显式 `const store = await getJobStore()` | v085 反例1、12（**真实 extract handler** 请求，非手工 IR） |
| 2 | 前端 Patch：管脚新增→`addPins`、删除→`removePins`、修改→稳定 `pinId`；支持 `pinsetId`、`landPattern=null`、可选尺寸 `null`；只提交真正变化的 figures；每次带 `expectedRevision`；`family` 改为服务端只读徽标 | 构建通过 + v085 反例4/10；**真实浏览器未运行（见 NOT VERIFIED）** |
| 3 | `lib/canonical.js`：Patch → strict validate → normalized IR → generate + `verifyConsistency` → `commitGeneration` 单事务保存 IR/revision/audit/manifest。失败路径不 commit | v085 反例2、3；PG-4 |
| 4 | `PKG_RANGE`/`LP_RANGE` 越界**直接 400**；`confirmed` 必须 boolean、`page` 正整数、`bbox` ∈[0,1] 且 x0<x1,y0<y1；addPins/removePins/resolveTransformations/evidence 各层未知字段 400 | v085 反例10 |
| 5 | EvidenceGate：`relevantFields` 与 landPattern 每字段缺锚点即 fail closed；管脚 `reviewerEdited` 必须有 reviewer 锚点；已确认图区必须有证据；reviewer 修改生成 `sourceType=reviewer` 锚点；`resolveTransformations` 必须带 reason+evidence | v085 反例7 |
| 6 | 阻断范围重划：几何/证据/家族 → footprint+3D；管脚 → symbol+footprint+3D；图区 → 仅 figures；全局问题（mock/未认证/校验错误）→ 全部 | v085 反例8 |
| 7 | `lib/lifecycle.js` 状态机 + `POST /api/lifecycle`（review/approve/publish/revoke 独立，角色分离，approve/publish 前重跑闸门校验） | v085 反例5 |
| 8 | Part Bundle 含完整 normalized package（全部几何 + landPattern + provenance + evidence + validationErrors）、pinsets（含墓碑与 resolution）、审批状态与文件清单；`reviewedBy` 只来自 `lifecycle.reviewedBy` | v085 反例2 |
| 9 | `assetFiles` 携带**真实文件内容**；`assetToken` 绑定 tenant+job+revision（15 分钟）；图区 PNG 由服务端写入文件清单并计入 manifest 哈希 | v085 反例9 |
| 10 | **真实 PostgreSQL 16.14 上运行**：迁移、跨实例共享、并发 CAS、事务回滚、幂等作用域、**自然过期释放 active 唯一索引**、撤销后重建、并发同键收敛 | `test/pg.test.js` 8/8（真跑，见下） |
| 11 | `readCookie` 安全处理畸形 Cookie（超长/无 '='/非法百分号/重复）不抛异常；exp/nbf 只接受**有限整数**（字符串、小数、Infinity、null 全拒） | v085 反例11 |
| 12 | `playwright.config.js` + `e2e/review-flow.spec.js`：Mock / Live-stub / Degraded 三模式，extract→改每类字段→增删管脚→generate→export→重新加载，比对页面/IR/数据库/KiCad/Bundle/Manifest | **未运行（NOT VERIFIED）** |
| 13 | README 新增「v0.8.5 生产闭环要点」并修正 postMessage 协议描述；.env.example 补 `ASSET_TOKEN_SECRET`、`TEST_DATABASE_URL` | — |

## 真实 PostgreSQL 测试（本轮真跑）

```
$ /usr/lib/postgresql/16/bin/psql ... -c "select version();"
PostgreSQL 16.14 (Ubuntu 16.14-0ubuntu0.24.04.1) on x86_64-pc-linux-gnu

$ TEST_DATABASE_URL="postgres://ds2k@localhost:5433/ds2kicad_test?host=/tmp" node --test test/pg.test.js
ok 1 - PG-1：迁移建表成功，且索引/约束就位
ok 2 - PG-2：create/get/update 跨"实例"共享（两个独立连接池）
ok 3 - PG-3：并发 update 只有一个成功（乐观锁 CAS）
ok 4 - PG-4：commitGeneration 事务性 —— 失败整体回滚
ok 5 - PG-5：幂等作用域 —— 同租户不同用户/不同文档不串 Job
ok 6 - PG-6：自然过期后同 Idempotency-Key 可重建（唯一索引不冲突）
ok 7 - PG-7：撤销后同 Idempotency-Key 可重建；撤销的作业不可读
ok 8 - PG-8：并发 create 同 Idempotency-Key 只产生一个 Job
# pass 8  # fail 0
```

## 全量测试（含 v0.8.4 全部用例，未删除）

```
$ npm test
# tests 118  # pass 110  # fail 0  # skipped 8
   （8 skip = 未设 TEST_DATABASE_URL 时的 PG 用例；设置后如上全跑）
$ node test/smoke.mjs           → 全部通过
$ VITE_EZPLM_ORIGINS=... npm run build → ✓ built (vite 8.2.0)
$ npm audit ; npm audit --omit=dev    → 0 vulnerabilities
```

因本轮语义变更（EvidenceGate fail closed、canPublish 依赖持久化批准、resolve 需证据），
v0.8.4 的 6 处旧断言已按新行为更新，**用例本身全部保留并继续运行**。

## NOT VERIFIED（未运行，不作任何声称）

- **真实浏览器 E2E（item 12）**：沙箱网络策略阻止 Playwright 下载 Chromium（`playwright install chromium` → Download failure）。`e2e/review-flow.spec.js` 已按要求写好三模式全流程，但**本轮未执行**。请在有外网的开发机运行：`npx playwright install chromium && npm run e2e`。**本报告不把"构造请求调用 handler"称作浏览器 E2E**——v085 全部用例走的是真实 HTTP handler，属于接口级 E2E，与页面级 E2E 是两件事。
- 前端 item 2 的改动仅经生产构建与代码审查，未在真实浏览器点击验证。
- 真实 Gemini 调用、KiCad 9/10 CLI/DRC/STEP/视觉回归、真实 OCR 引擎识别质量、多厂商语料与任何 benchmark 数字：均未运行。
- ezPLM 真实 IdP 联调、对象存储预签名 URL、DNS rebinding 残余窗口：未实现/未验证。
- PG 测试在本地 Unix socket 单机实例上运行；**云托管 PG（连接数限制、SSL、pgbouncer 事务模式）未验证**。

## 部署必读

生产环境必填：`DATABASE_URL`（PostgreSQL，且不得设 `JOBSTORE_FILE`）、`EZPLM_JWT_SECRET`、`EZPLM_JWT_ISS`、`EZPLM_JWT_AUD`、`PDF_TOKEN_SECRET`、`ASSET_TOKEN_SECRET`（可复用前者）、`VITE_EZPLM_ORIGINS`。
新增接口 `POST /api/lifecycle`；`canPublish` 语义变更为依赖持久化批准，前端仅显示不再据此放行。
