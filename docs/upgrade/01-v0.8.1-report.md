# v0.8.1 修复报告（只修 P0，不扩展功能）

版本来源统一：`package.json` / `package-lock.json` 均为 0.8.1。

## 12 项逐条

| # | 要求 | 落地 | 证据 |
|---|---|---|---|
| 1 | 唯一 PromotionGate | `lib/promotion.js`：mock/missing/default/clamped/derived/geometry_fix/pin_conflict/unsupported_pkg/no_output/unverified_edit 十类原因统一塌陷为 `nonPromotable` | test/v081 #1 #3 |
| 2 | mock 全链路 | extract 返回 `mock`+`non_promotable` → App 透传 `mock` 至 generate → bundle.mock → part-bundle.mock → postMessage.mock/nonPromotable/reasons | test/v081 #1 |
| 3 | 删除 generateAll 旁路 | `generateAll` 改为薄适配层，内部调用 `generateBundle`，同一闸门；无第二条生成路径 | test/v081 #2 |
| 4 | Gemini 禁估算 | 删除全部 "reasonable/JEDEC-standard nominal"；schema 示例数字改为 `<mm\|null>` 类型占位并显式声明"占位符不是值" | test/v081 #10 |
| 5 | 禁静默 clamp | `sanitizePackage` 产出 `fieldProvenance{source,rawValue,normalizedValue,reason}`，source ∈ datasheet/missing/clamped/reviewer/not_applicable；clamped 保留 rawValue 且不可晋升 | test/v081 #3 |
| 6 | 矩形 QFN/DFN | 左右列用 `bodyWidth`、上下行用 `bodyLength`（此前两轴都用 bodyLength）；BGA/LCCC/PLCC 列入 UNSUPPORTED 只出符号；QFN 非 4 倍数由"回退 dual 近似"改为阻断 | test/v081 #3 #4 |
| 7 | 移除公开 Edge 代理 | `api/fetch-pdf.js` 改 Node runtime + SafeDownloader（逐跳校验）+ HMAC 短期令牌（绑定 URL，15 分钟），令牌由 extract 校验通过后签发 | test/v081 #5 #6 |
| 8 | postMessage 严格 | 入站校验 origin 白名单 + `event.source===window.parent` + nonce + jobId；出站精确 targetOrigin 并回带 nonce/jobId，无会话拒发；生产构建缺 `VITE_EZPLM_ORIGINS` 直接失败 | test/v081 #7 |
| 9 | 删除 VITE_API_TOKEN | 前端不再注入任何令牌，改 `credentials:'same-origin'`；构建产物全文搜索无 API_TOKEN | test/v081 #8 |
| 10 | reviewer provenance | `applyReviewerEdit(pkg, field, value, reviewer, reason)` 写入 reviewer/at/reason 并重算 missingFields；无署名或越界值仍不可晋升；UI 增审核者标识输入 | test/v081 #9 |
| 11 | pdf-inspector OCR 路由 | `PDF_PARSER=inspector` 时调 `classify()` 取 `pagesNeedingOcr`；选页改为"剔除需 OCR 页 → 关键词命中页 → 首页+有文本尾页"，删除"前6页+后8页"盲切；全扫描文档标记 `scanned_no_ocr_worker` 不盲切 | 代码；真实扫描件回归 **NOT VERIFIED**（无语料） |
| 12 | 文档/版本/目录 | 版本三处统一；`docs/upgrade/00-v0.8.0-p0-report.md` 更名去除误导；.env.example 补 `PDF_TOKEN_SECRET`、删 `VITE_API_TOKEN` | — |

## 测试结果（真实命令与输出）

```
$ npm test           # node --test test/*.test.js
# tests 59  # pass 59  # fail 0
$ node test/smoke.mjs
全部通过（23 项）
$ VITE_EZPLM_ORIGINS=https://ezplm.cn npm run build
✓ built
```

## 明确 NOT VERIFIED（未运行，不作任何声称）

- **KiCad 9/10 CLI 加载、DRC、STEP 导出、视觉回归**：本环境无 KiCad，未运行。
- **真实 Gemini 调用**：无 API Key，未运行；提示词改动仅经源码断言验证。
- **多厂商真实数据手册回归 / 200 份 benchmark**：无语料库，未运行。
- **pdf-inspector 在真实扫描件上的 OCR 路由准确率**：仅有合成 PDF 契约测试（本机 Linux x64 加载成功），未做准确率评估。
- **对象存储 + 预签名 URL**：本仓库无状态部署，item 7 以 HMAC 短期令牌等价实现；迁入 ezPLM 后台后应替换为对象存储预签名 URL。
- **DNS rebinding 残余窗口**：校验与连接之间仍有时间窗，需按解析 IP 直连（undici 定制），未实现。
